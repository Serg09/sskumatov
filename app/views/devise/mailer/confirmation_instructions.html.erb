<!DOCTYPE html>

<html>
  <body>
    <p>Welcome <%= @resource.name %>,</p>

    <p>You can confirm your account email <%= @resource.email %> through the link below:</p>

    <b><p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token) %></p></b>
  </body>
</html>