<p>Welcome <%= @resource.name %>,

<p>You can confirm your account email <%= @email %> through the link below:</p>

<p><strong><<%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token) %></p></strong>
